services:
  php:
    build: 
      context: .
      dockerfile: .devcontainer/Dockerfile_php
    volumes: 
      - .:/workspace/bienesraices
    ports:
      - "3000:3000"
    depends_on:
      - db
    tty: true
    env_file:
      - .env

  db:
    image: mysql:8.0
    restart: always
    command: --ssl=0
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - db:/var/lib/mysql

  nodejs:
    image: node:24.6.0
    container_name: nodejs
    build: 
      context: .
      dockerfile: .devcontainer/Dockerfile_nodejs
    volumes:
      - .:/workspace/bienesraices:delegated
    ports:
      - "5500:5500"
    depends_on:
      - db
      - php
    env_file:
      - .env

volumes:
  db:
    external: true